import 'package:flakka_protobuf_build_types/buf/alpha/image/v1/image.pb.dart';
import 'package:test/test.dart';

void main() {
  test('ensure coverage data for lcov', () {
    expect(Image(), Image());
  });
}
